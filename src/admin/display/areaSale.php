action areaSale.php

sql get all areas where sale

filteredList=sqlresult


if(POST[priceFrom] && priceTo
{
	for i filteredList i++
		if(!POST[name] operation filteredList[i])
			filteredList.remove(filteredList[i]
		
}
elseif(...)
{
	iterate filteredList
		if(!)
			filteredList.remove(filteredList[i])
			
}

SESSION[saleFilter]=filteredList;
